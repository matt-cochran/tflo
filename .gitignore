# ── Build artifacts ────────────────────────────────────────────────────
/target/
/tflo-wasm/pkg/

# ── FrontRails / StructureOS / IntentOS scratch state ──────────────────
# MCP tools store per-workspace caches, backups, and llm context here.
/.frontrails/
/.structureos/

# ── tflo-site ──────────────────────────────────────────────────────────
# Note: /tflo-site/public/wasm/ is intentionally tracked so Cloudflare
# Pages (which has no wasm-pack) can serve the prebuilt artifact. Rebuild
# locally via `npm run build:wasm` and commit when Rust changes.
/tflo-site/.astro/
/tflo-site/dist/
/tflo-site/node_modules/

# ── IDE / editor ───────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── OS ─────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.Zone.Identifier

# ── Python (fixture generators in tflo-fintech/tests/golden/scripts/) ─
__pycache__/
*.pyc

# ── Misc ───────────────────────────────────────────────────────────────
*.log
*.bak
